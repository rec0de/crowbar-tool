module Create;

data Spec = ObjInv(Bool) | Ensures(Bool) | Requires(Bool) | WhileInv(Bool);

interface I{ }

[Spec : Requires(this.k > 0)]
[Spec : ObjInv(this.k > 0)]
class C(I one, Int k) implements I {

	Int fail(){  	
	    I two = new C(one, k-1);
	    return 0;
	}	
	
	Int success(){  	
	   new C(one, k+1);
	    return 0;
	}
	
}

{

}