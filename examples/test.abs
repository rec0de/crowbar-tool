module Test;

data Spec = ObjInv(Bool) | Ensures(Bool) | Requires(Bool) | WhileInv(Bool);

class C( Int f ) {
	Int g= 0;
    
    Unit m(Int v){
        this.g = this.f;
        this.f = v;
        v = this.g;
    }
}

{ new C(1); }
